{% extends "Setup/Academic/index.html.twig" %}
{% block page_header %} Academic Session {% endblock %}
{% block breadcurm_nav_list %}
    {{ render_header_list(path('vx_academic_session_add'), 'Add Academic Session', 'fas fa-plus-circle') }}
{% endblock %}
{% block content_academic %}
    <div class="panel panel-flat">
        <div class="panel-body">
            <div class="table-responsive">
                <table class="custom-table table  table-hover">
                    <thead class="bg-table-head">
                    <tr class="border-double">
                        <th class="sn text-semibold ">#</th>
                        <th class="text-semibold ">Name</th>
                        <th class="text-semibold ">Code</th>
                        <th class="text-semibold  action text-center">Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% set counter = 1 %}
                    {% for academicSession in academicSessions %}
                    <tr>
                        <td>{{ counter }}</td>
                        <td>{{ academicSession.name }}</td>
                        <td>{{ academicSession.code }}</td>
                        <td class="action text-center">
                            <ul class="icons-list">
                                {{ render_action_button(path('vx_setup_academic_session_update', {'id':academicSession.id }), '','fas fa-pencil-alt', 'text-info' ) }}
                                {{ render_action_button(path('vx_delete_academic_session', {'id':academicSession.id }), '','fas fa-trash-alt', 'text-danger' ) }}
                            </ul>
                        </td>
                    </tr>
                        {% set counter = counter + 1 %}
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
{% block scripts %}
    <script>
        $('.modalTest').fnAjaxModal();


    </script>

{% endblock %}